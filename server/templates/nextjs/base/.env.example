# NextAuth configuration
#
# The canonical URL of your app.
# Local dev default:
NEXTAUTH_URL=http://localhost:3000

# A strong secret used to sign/encrypt NextAuth tokens & cookies.
# Generate one:
#   openssl rand -base64 32
NEXTAUTH_SECRET=

# Database connection string.
# This starter uses Postgres via the `pg` driver.
# Examples:
#   postgres://user:password@localhost:5432/devstarter
#   (Vercel Postgres / Neon / Supabase) use the provided connection URL
DATABASE_URL=
