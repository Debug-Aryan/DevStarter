# ------------------------------
# DevStarter Node + Express API
# ------------------------------

# NODE_ENV
# Set to "development" for local dev, "production" for deployments.
NODE_ENV=development

# PORT
# Port the server listens on.
PORT=5000

# MONGO_URI
# MongoDB connection string (required for auth/user features).
# Example: mongodb+srv://<user>:<pass>@cluster.mongodb.net/<db>?retryWrites=true&w=majority
MONGO_URI=

# JWT_SECRET
# Secret used to sign JWTs. Use a long random string and NEVER commit real secrets.
JWT_SECRET=

# JWT_EXPIRES_IN
# Token lifetime. Examples: 15m, 1h, 7d, 15d
JWT_EXPIRES_IN=15d

# CLIENT_URL
# Allowed origin(s) for CORS. Supports multiple, comma-separated.
# Example: http://localhost:5173,https://your-frontend.com
CLIENT_URL=http://localhost:5173
