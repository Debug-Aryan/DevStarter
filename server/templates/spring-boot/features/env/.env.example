# DevStarter Spring Boot environment variables
# Copy this file to `.env` for local development (optional) and load it with your shell.
# NOTE: Spring Boot reads environment variables automatically; no extra library is required.

# Server
SERVER_PORT=8080

# Database
# Use a JDBC URL.
# - Local Postgres example: jdbc:postgresql://localhost:5432/devstarter
# - Local H2 (in-memory) is the default if DATABASE_URL is not set.
DATABASE_URL=
DB_USERNAME=
DB_PASSWORD=

# JWT
# Use a long random secret (recommended: 32+ bytes).
# You can provide either a raw string or a base64-encoded value.
JWT_SECRET=

# Token lifetime in milliseconds (example: 86400000 = 24h)
JWT_EXPIRATION=86400000
