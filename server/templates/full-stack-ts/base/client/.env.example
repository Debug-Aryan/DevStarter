# Optional. If empty, the client defaults to `/api`.
# For local dev, Vite proxies `/api` to the server.
# For production, set this to your deployed server base URL INCLUDING `/api`.
# Example: https://your-server.onrender.com/api
VITE_API_URL=
