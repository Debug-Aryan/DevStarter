# Server environment variables
#
# NODE_ENV: "development" or "production".
NODE_ENV=development

# PORT: Express server port.
PORT=5000

# MONGO_URI: MongoDB connection string.
# Example: mongodb://localhost:27017/myapp
MONGO_URI=mongodb://localhost:27017/__PROJECT_NAME__

# JWT_SECRET: secret key used to sign JWT tokens (keep this private!).
JWT_SECRET=replace_me_with_a_long_random_string

# JWT_EXPIRES_IN: token lifetime in jsonwebtoken format (e.g. "7d", "1h").
JWT_EXPIRES_IN=7d

# CLIENT_URL: frontend origin for CORS (Vite dev default is http://localhost:5173)
CLIENT_URL=http://localhost:5173
