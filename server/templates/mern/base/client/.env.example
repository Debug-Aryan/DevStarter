# Base URL for the backend API.
#
# Local dev (recommended):
# - If you use Vite proxy (vite.config.js) you can set this to "/api".
# - Or point directly to your server like "http://localhost:5000/api".
#
# Production:
# - Set to your deployed API origin + /api.
VITE_API_URL=/api
