# __PROJECT_NAME__

__PROJECT_DESCRIPTION__

This is a production-ready MERN starter generated by **DevStarter**.

## Tech Stack
- **Frontend:** React + Vite + Tailwind CSS
- **Backend:** Node.js + Express
- **Database:** MongoDB + Mongoose
- **Auth:** JWT (httpOnly cookie) + protected routes

## Features
__FEATURES_LIST__

## Folder Structure

### Client
```text
client/
   postcss.config.cjs
   tailwind.config.cjs
   src/
      app/
         App.jsx
         router.jsx
         components/
            AppShell.jsx
         pages/
            HomePage.jsx
            DashboardPage.jsx
      components/
         common/
            Loader.jsx
            ErrorMessage.jsx
      context/
         AuthContext.jsx
      features/
         auth/
            components/
               AuthCard.jsx
            hooks/
               useAuth.js
            pages/
               LoginPage.jsx
               RegisterPage.jsx
            services/
               authApi.js
      services/
         api.js
      styles/
         index.css
      index.css
      main.jsx
```

### Server
```text
server/
   server.js
   src/
      app.js
      config/
         db.js
      controllers/
         authController.js
         healthController.js
      middleware/
         authMiddleware.js
         errorMiddleware.js
      models/
         User.js
      routes/
         index.js
         authRoutes.js
         healthRoutes.js
      services/
         authService.js
         userService.js
      utils/
         response.js
         token.js
```

## Getting Started (Local)

### Prerequisites
- Node.js **18+**
- MongoDB running locally OR a MongoDB Atlas connection string

### 1) Install dependencies
```bash
cd server
npm install

cd ../client
npm install
```

### 2) Configure environment variables

#### Server env
```bash
cd server
cp .env.example .env
```

Set at least:
- `MONGO_URI`
- `JWT_SECRET`
- `CLIENT_URL` (usually `http://localhost:5173`)

#### Client env
```bash
cd client
cp .env.example .env
```

Set:
- `VITE_API_URL` (recommended for dev: `/api`)

### 3) Run the app

In one terminal:
```bash
cd server
npm run dev
```

In another terminal:
```bash
cd client
npm run dev
```

Open: `http://localhost:5173`

## API Overview

All API responses follow:
```json
{
   "success": true,
   "message": "...",
   "data": {}
}
```

Key routes:
- `GET /api/health` → service health (includes uptime + timestamp)
- `POST /api/auth/register` → create account + sets auth cookie
- `POST /api/auth/login` → login + sets auth cookie
- `POST /api/auth/logout` → clears auth cookie
- `GET /api/auth/me` → returns the current user (protected)

## Auth Flow (Beginner-friendly)

1. User registers/logs in on the client.
2. Server creates a JWT and stores it in an **httpOnly cookie** named `token`.
3. Client uses `axios` with `withCredentials: true` so cookies are sent automatically.
4. Protected server routes use `requireAuth` middleware to verify the JWT.

## Tailwind CSS Notes

Tailwind is configured in:
- `client/tailwind.config.cjs`
- `client/postcss.config.cjs`
- `client/src/index.css` (includes `@tailwind base/components/utilities`)

If you see unstyled pages, confirm:
- `client/src/main.jsx` imports `./index.css`
- `tailwind.config.cjs` has the correct `content` paths

## Deployment Notes

- Configure `CLIENT_URL` on the server to match your deployed frontend origin.
- Configure `VITE_API_URL` on the client to match your deployed backend `/api` URL.
- If you deploy frontend and backend on different domains and need cross-site cookies,
   you may need to adjust CORS and cookie `sameSite/secure` settings in `server/src/utils/token.js`.
